{
  "name": "ghost-builder-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.51.0"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
